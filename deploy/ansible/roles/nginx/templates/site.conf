server {
  listen 80;
  server_name *.{{fqdn}};
  rewrite ^(.*)$ $scheme://{{fqdn}}$1 permanent;
}

server {
  listen 80;
  include gzip_params;

  server_name {{fqdn}};

  location / {
    index index.html;
    root {{public_path}};
    try_files $uri /index.html;
  }
}
